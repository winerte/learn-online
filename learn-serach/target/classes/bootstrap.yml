spring:
  application:
    name: learn-search
  cloud:
    config:
    #http://localhost:7500/qf-shop/dev
      #uri: http://localhost:7500
      discovery:
      #我们配置再eureka上 得唯一标识
        service-id: qfshop-config
        enabled: true
      name: qf-shop
      profile: dev
server:
  port: 8010
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka